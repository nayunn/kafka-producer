version: 0.0
os: linux

files:
  - source: /
    destination: /src/kafka-producer
    overwrite: yes

file_exists_behavior: OVERWRITE

permissions:
  - object: /src/kafka-producer
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mode: 755

hooks:
  BeforeInstall:
    - location: kafka-producer/deploy/before_install.sh
      timeout: 60
      runas: ubuntu

  AfterInstall:
    - location: kafka-producer/deploy/after_install.sh
      timeout: 300
      runas: ubuntu

  ApplicationStart:
    - location: kafka-producer/deploy/application_start.sh
      timeout: 300
      runas: ubuntu

  ValidateService:
    - location: kafka-producer/deploy/validate.sh
      timeout: 60
      runas: ubuntu
